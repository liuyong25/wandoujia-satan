define([
        'angular',
        'modules/photos'
    ], function(
        angular,
        photos
    ) {
    'use strict';
    angular.bootstrap(document, ['wdjPhotos']);
    return 'Hello from Yeoman!';
});
