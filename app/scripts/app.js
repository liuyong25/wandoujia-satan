define([
        'angular',
        'modules/app'
    ], function(
        angular,
        app
    ) {
    'use strict';
    angular.bootstrap(document, ['wdApp']);
    return 'Hello from Yeoman!';
});
