define([
        'angular',
        'modules/photos'
    ], function(
        angular,
        photos
    ) {
    'use strict';
    angular.bootstrap(document, ['wdPhotos']);
    return 'Hello from Yeoman!';
});
